/** @typedef {import('pg').Pool} PgPool */

module.exports = {
    /** @param {PgPool} db */
    up: async (db) => {
        // TODO: make changes
    },

    /** @param {PgPool} db */
    down: async (db) => {
        // TODO: revert changes
    },
};